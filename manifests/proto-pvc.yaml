apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: proto-pvc
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard
  volumeMode: Filesystem
